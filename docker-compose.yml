services:
  # ============================================================
  # PostgreSQL - Source of Truth (Work Items, RCA, History)
  # ============================================================
  postgres:
    image: postgres:16-alpine
    container_name: ims-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ims
      POSTGRES_PASSWORD: ims_secret
      POSTGRES_DB: ims_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backend/sql:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ims -d ims_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ============================================================
  # MongoDB Replica Set - Data Lake (Raw Signals, Audit Log)
  # ============================================================
  mongo1:
    image: mongo:7
    container_name: ims-mongo1
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: ims
      MONGO_INITDB_ROOT_PASSWORD: ims_secret
      MONGO_INITDB_DATABASE: ims
    ports:
      - "27017:27017"
    volumes:
      - mongo1_data:/data/db
      - ./backend/mongo/mongo-keyfile:/etc/mongo-keyfile-src:ro
    command: >
      bash -c "cp /etc/mongo-keyfile-src /tmp/mongo-keyfile
      && chmod 400 /tmp/mongo-keyfile
      && chown 999:999 /tmp/mongo-keyfile
      && exec docker-entrypoint.sh mongod --replSet rs0 --bind_ip_all --keyFile /tmp/mongo-keyfile --port 27017"
    healthcheck:
      test: ["CMD", "mongosh", "mongodb://ims:ims_secret@localhost:27017/admin?authSource=admin", "--quiet", "--eval", "db.adminCommand({ ping: 1 }).ok"]
      interval: 10s
      timeout: 5s
      retries: 5

  mongo2:
    image: mongo:7
    container_name: ims-mongo2
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: ims
      MONGO_INITDB_ROOT_PASSWORD: ims_secret
      MONGO_INITDB_DATABASE: ims
    ports:
      - "27018:27017"
    volumes:
      - mongo2_data:/data/db
      - ./backend/mongo/mongo-keyfile:/etc/mongo-keyfile-src:ro
    command: >
      bash -c "cp /etc/mongo-keyfile-src /tmp/mongo-keyfile
      && chmod 400 /tmp/mongo-keyfile
      && chown 999:999 /tmp/mongo-keyfile
      && exec docker-entrypoint.sh mongod --replSet rs0 --bind_ip_all --keyFile /tmp/mongo-keyfile --port 27017"
    healthcheck:
      test: ["CMD", "mongosh", "mongodb://ims:ims_secret@localhost:27017/admin?authSource=admin", "--quiet", "--eval", "db.adminCommand({ ping: 1 }).ok"]
      interval: 10s
      timeout: 5s
      retries: 5

  mongo3:
    image: mongo:7
    container_name: ims-mongo3
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: ims
      MONGO_INITDB_ROOT_PASSWORD: ims_secret
      MONGO_INITDB_DATABASE: ims
    ports:
      - "27019:27017"
    volumes:
      - mongo3_data:/data/db
      - ./backend/mongo/mongo-keyfile:/etc/mongo-keyfile-src:ro
    command: >
      bash -c "cp /etc/mongo-keyfile-src /tmp/mongo-keyfile
      && chmod 400 /tmp/mongo-keyfile
      && chown 999:999 /tmp/mongo-keyfile
      && exec docker-entrypoint.sh mongod --replSet rs0 --bind_ip_all --keyFile /tmp/mongo-keyfile --port 27017"
    healthcheck:
      test: ["CMD", "mongosh", "mongodb://ims:ims_secret@localhost:27017/admin?authSource=admin", "--quiet", "--eval", "db.adminCommand({ ping: 1 }).ok"]
      interval: 10s
      timeout: 5s
      retries: 5

  mongo-init-replica:
    image: mongo:7
    container_name: ims-mongo-init-replica
    restart: "no"
    environment:
      MONGO_INITDB_ROOT_USERNAME: ims
      MONGO_INITDB_ROOT_PASSWORD: ims_secret
      MONGO_REPLICA_SET_NAME: rs0
    depends_on:
      mongo1:
        condition: service_healthy
      mongo2:
        condition: service_healthy
      mongo3:
        condition: service_healthy
    volumes:
      - ./backend/mongo/init_replica.sh:/scripts/init_replica.sh:ro
      - ./backend/mongo/init_indexes.js:/scripts/init_indexes.js:ro
    command: ["bash", "/scripts/init_replica.sh"]

  # ============================================================
  # Redis - Rate Limiter + Debounce Control Plane
  # ============================================================
  redis-rate-limit-debounce:
    image: redis:7-alpine
    container_name: ims-redis-rate-limit-debounce
    restart: unless-stopped
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy volatile-lru
    ports:
      - "6379:6379"
    volumes:
      - redis_rate_limit_debounce_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ============================================================
  # Redis - Streams Plane (Ingest + Audit Streams)
  # ============================================================
  redis-streams:
    image: redis:7-alpine
    container_name: ims-redis-streams
    restart: unless-stopped
    command: redis-server --appendonly yes --maxmemory 512mb --maxmemory-policy noeviction
    ports:
      - "6380:6379"
    volumes:
      - redis_streams_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ============================================================
  # Redis - Dashboard Cache
  # ============================================================
  redis-dashboard-cache:
    image: redis:7-alpine
    container_name: ims-redis-dashboard-cache
    restart: unless-stopped
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
    ports:
      - "6381:6379"
    volumes:
      - redis_dashboard_cache_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ============================================================
  # Redis Commander - Redis UI (Development Only)
  # ============================================================
  redis-commander:
    image: rediscommander/redis-commander:latest
    container_name: ims-redis-commander
    restart: unless-stopped
    environment:
      REDIS_HOSTS: control:redis-rate-limit-debounce:6379,streams:redis-streams:6379,dashboard:redis-dashboard-cache:6379
    ports:
      - "8081:8081"
    depends_on:
      - redis-rate-limit-debounce
      - redis-streams
      - redis-dashboard-cache
    profiles:
      - dev

  # ============================================================
  # Mongo Express - MongoDB UI (Development Only)
  # ============================================================
  mongo-express:
    image: mongo-express:latest
    container_name: ims-mongo-express
    restart: unless-stopped
    environment:
      ME_CONFIG_MONGODB_ADMINUSERNAME: ims
      ME_CONFIG_MONGODB_ADMINPASSWORD: ims_secret
      ME_CONFIG_MONGODB_URL: mongodb://ims:ims_secret@mongo1:27017,mongo2:27017,mongo3:27017/ims?authSource=admin&replicaSet=rs0
      ME_CONFIG_BASICAUTH_USERNAME: admin
      ME_CONFIG_BASICAUTH_PASSWORD: admin
    ports:
      - "8082:8081"
    depends_on:
      mongo-init-replica:
        condition: service_completed_successfully
    profiles:
      - dev

  # ============================================================
  # Backend API
  # ============================================================
  backend-api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      APP_ENV: docker
      APP_ROLE: api
      NODE_ENV: production
      PORT: 3000
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_USER: ims
      POSTGRES_PASSWORD: ims_secret
      POSTGRES_DB: ims_db
      MONGODB_URI: mongodb://ims:ims_secret@mongo1:27017,mongo2:27017,mongo3:27017/ims?authSource=admin&replicaSet=rs0
      REDIS_CONTROL_HOST: redis-rate-limit-debounce
      REDIS_CONTROL_PORT: 6379
      REDIS_STREAMS_HOST: redis-streams
      REDIS_STREAMS_PORT: 6379
      REDIS_DASHBOARD_HOST: redis-dashboard-cache
      REDIS_DASHBOARD_PORT: 6379
      RATE_LIMIT_BUCKET_CAPACITY: 10000
      RATE_LIMIT_REFILL_RATE: 10000
      DEBOUNCE_TTL_SEC: 10
    ports:
      - "3000:3000"
    depends_on:
      postgres:
        condition: service_healthy
      mongo-init-replica:
        condition: service_completed_successfully
      redis-rate-limit-debounce:
        condition: service_healthy
      redis-streams:
        condition: service_healthy
      redis-dashboard-cache:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # ============================================================
  # Signal Worker
  # ============================================================
  backend-signal-worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      APP_ENV: docker
      APP_ROLE: signal-worker
      NODE_ENV: production
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_USER: ims
      POSTGRES_PASSWORD: ims_secret
      POSTGRES_DB: ims_db
      MONGODB_URI: mongodb://ims:ims_secret@mongo1:27017,mongo2:27017,mongo3:27017/ims?authSource=admin&replicaSet=rs0
      REDIS_CONTROL_HOST: redis-rate-limit-debounce
      REDIS_CONTROL_PORT: 6379
      REDIS_STREAMS_HOST: redis-streams
      REDIS_STREAMS_PORT: 6379
      REDIS_DASHBOARD_HOST: redis-dashboard-cache
      REDIS_DASHBOARD_PORT: 6379
      RATE_LIMIT_BUCKET_CAPACITY: 10000
      RATE_LIMIT_REFILL_RATE: 10000
      DEBOUNCE_TTL_SEC: 10
    depends_on:
      postgres:
        condition: service_healthy
      mongo-init-replica:
        condition: service_completed_successfully
      redis-rate-limit-debounce:
        condition: service_healthy
      redis-streams:
        condition: service_healthy
      redis-dashboard-cache:
        condition: service_healthy

  # ============================================================
  # Audit Worker
  # ============================================================
  backend-audit-worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      APP_ENV: docker
      APP_ROLE: audit-worker
      NODE_ENV: production
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_USER: ims
      POSTGRES_PASSWORD: ims_secret
      POSTGRES_DB: ims_db
      MONGODB_URI: mongodb://ims:ims_secret@mongo1:27017,mongo2:27017,mongo3:27017/ims?authSource=admin&replicaSet=rs0
      REDIS_CONTROL_HOST: redis-rate-limit-debounce
      REDIS_CONTROL_PORT: 6379
      REDIS_STREAMS_HOST: redis-streams
      REDIS_STREAMS_PORT: 6379
      REDIS_DASHBOARD_HOST: redis-dashboard-cache
      REDIS_DASHBOARD_PORT: 6379
      RATE_LIMIT_BUCKET_CAPACITY: 10000
      RATE_LIMIT_REFILL_RATE: 10000
      DEBOUNCE_TTL_SEC: 10
    depends_on:
      postgres:
        condition: service_healthy
      mongo-init-replica:
        condition: service_completed_successfully
      redis-rate-limit-debounce:
        condition: service_healthy
      redis-streams:
        condition: service_healthy
      redis-dashboard-cache:
        condition: service_healthy

  # ============================================================
  # Frontend (uncomment when ready)
  # ============================================================
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_URL: http://localhost:3000/api
    container_name: ims-frontend
    restart: unless-stopped
    ports:
      - "5173:80"
    depends_on:
      - backend-api

volumes:
  postgres_data:
  mongo1_data:
  mongo2_data:
  mongo3_data:
  redis_rate_limit_debounce_data:
  redis_streams_data:
  redis_dashboard_cache_data:

networks:
  default:
    name: ims-network

